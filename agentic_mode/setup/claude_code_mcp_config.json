{
  "mcpServers": {
    "blender": {
      "command": "uvx",
      "args": ["blender-mcp"],
      "description": "Connect to a running Blender 4.5+ instance via the Blender MCP add-on. Start the Blender MCP server from the add-on panel BEFORE opening Claude Code."
    }
  }
}
